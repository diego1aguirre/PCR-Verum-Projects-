{
  "name": "pcr-verum-projects",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd verum-tools && npm install && npm run build",
    "start": "node verum-tools/server.js"
  }
}
